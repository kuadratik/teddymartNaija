<?php

namespace Illuminate\Http {
    /**
     * @method isBool(string $key) Check if the given key is boolean. Returns true when value is "1", "0", "true", "false". | Check the AppServiceProvider to see implementation.
     */
    class Request 
    {
        //
    }
}